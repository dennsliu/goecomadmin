import React from 'react';
import styles from './AddUser.less';

const AddUser: React.FC = () => {
  const handleSubmit = async (values: API.LoginParams) => {
    try {
      // 登录
    } catch (error) {}
  };

  return <div className={styles.container}>tutyutyuty</div>;
};

export default AddUser;
