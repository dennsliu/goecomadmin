import React from 'react';
import styles from './Users.less';

const Users: React.FC = () => {
  const handleSubmit = async (values: API.LoginParams) => {
    try {
      // 登录
    } catch (error) {}
  };

  return <div className={styles.container}>Merchant User List</div>;
};

export default Users;
